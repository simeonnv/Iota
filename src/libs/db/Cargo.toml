[package]
name = "db"
version = "0.1.0"
edition = "2024"

[dependencies]
lazy_static = "1.5.0"
sqlx = { version = "0.8", features = [
    "runtime-tokio",
    "postgres",
    "chrono",
    "uuid",
] }
error = { path = "../error" }
uuid = { version = "1.18.1", features = ["v4"] }
log = "0.4.27"
